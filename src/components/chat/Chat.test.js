//Test Driven Development for Chat

import Chat from "./Chat";

import { render, fireEvent, screen  } from "@testing-library/react";

import '@testing-library/jest-dom/extend-expect';

//test chat members
/*describe(Chat, () => {
    it("chat members is an empty array when nothing is passed to it", () => {
        

    })

    it("chat members should be a list of users when users are passed it it", () => {
        const mockGroupMembers = [
            { id: 'user1', name: 'Alice' },
            { id: 'user2', name: 'Bob' },
            { id: 'user3', name: 'Charlie' },
        ];
        render(<Chat groupMembers={mockGroupMembers} />);
  
        // Check if each member is rendered
        mockGroupMembers.forEach(member => {
            expect(screen.getByText(member.name)).toBeInTheDocument();
        });

    })
})
*/

//test Group name


//test msg

//test usernmae

